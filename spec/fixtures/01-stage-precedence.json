{
  "name": "01-stage-precedence",
  "description": "Rows at every work stage at once. Precedence is fixing > spec-ready > spec-draft > new > discover, so the served Stage is `fixing` even though `new` rows are older in file order. `pr-open` is counted and reported as a cheap poll but is never served as a Stage; `blocked` is counted and is neither.",
  "input": {
    "queue": [
      { "finding": "oldest umbrella ticket", "source": "gh#101", "priority": "low", "spec": "", "status": "new" },
      { "finding": "parser drops piped cells", "source": "gh#102", "priority": "high", "spec": "specs/parser.md", "status": "spec-draft" },
      { "finding": "offload rewrite", "source": "gh#103", "priority": "medium", "spec": "specs/bash-output-offload.md", "status": "spec-ready" },
      { "finding": "judge runner", "source": "gh#104", "priority": "high", "spec": "specs/pairwise-judge-verdicts.md", "status": "fixing" },
      { "finding": "release automation", "source": "gh#105", "priority": "medium", "spec": "specs/release.md", "status": "pr-open" },
      { "finding": "pricing tier rename", "source": "gh#106", "priority": "high", "spec": "", "status": "blocked" }
    ],
    "policy": { "concepts": [] },
    "journal": []
  },
  "expected": {
    "stage": "fixing",
    "next": "CONTINUE",
    "counts": { "pr-open": 1, "fixing": 1, "spec-ready": 1, "spec-draft": 1, "new": 1, "blocked": 1 },
    "targets": [ { "stage": "fixing", "finding": "judge runner", "source": "gh#104" } ],
    "events": [ { "event": "stage", "stage": "fixing", "scope": "" } ],
    "messages": []
  }
}
